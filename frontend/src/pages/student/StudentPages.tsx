import { BookOpen, CheckCircle, EnvelopeSimple as Mail, GraduationCap, Heartbeat, Lifebuoy, Pulse as Activity, Phone, ShieldCheck, Wind, X } from "@phosphor-icons/react"; import { useEffect, useState } from "react"; import { Link, useNavigate } from "react-router-dom"; import { SOCKET_EVENTS, type PendingEmergency, type SessionMatch } from "@bodhi/shared"; import { Button } from "../../components/ui/Button"; import { useAuth } from "../../context/AuthContext"; import { api } from "../../lib/api"; import { getSocket } from "../../lib/socket";
const moods=[{label:"Anxious",symbol:"😰",tone:"yellow"},{label:"Depressed",symbol:"😔",tone:"blue"},{label:"Overwhelmed",symbol:"😵",tone:"red"},{label:"Angry",symbol:"😠",tone:"orange"},{label:"Confused",symbol:"🤔",tone:"purple"},{label:"Just need to talk",symbol:"💬",tone:"green"}];
export function StudentOverview(){const{user}=useAuth();const navigate=useNavigate();const[history,setHistory]=useState<any[]>([]);const[modal,setModal]=useState(false);const[mood,setMood]=useState("");const[urgent,setUrgent]=useState(false);const[waiting,setWaiting]=useState(false);const[sending,setSending]=useState(false);const[toast,setToast]=useState("");useEffect(()=>{api<any>("/student/history").then(r=>setHistory(r.requests)).catch(()=>{});const socket=getSocket();const queued=(_r:PendingEmergency)=>{setSending(false);setWaiting(true);setModal(false);setToast("Request sent. A psychologist will connect shortly.")};const matched=(match:SessionMatch)=>navigate(`/student/session/${match.sessionId}`,{state:{...match,mood,urgent}});socket.on(SOCKET_EVENTS.EMERGENCY_QUEUED,queued);socket.on(SOCKET_EVENTS.SESSION_MATCHED,matched);return()=>{socket.off(SOCKET_EVENTS.EMERGENCY_QUEUED,queued);socket.off(SOCKET_EVENTS.SESSION_MATCHED,matched)}},[navigate,mood,urgent]);useEffect(()=>{if(!toast)return;const timer=setTimeout(()=>setToast(""),4000);return()=>clearTimeout(timer)},[toast]);function request(){setSending(true);getSocket().emit(SOCKET_EVENTS.EMERGENCY_REQUEST,{mode:"chat",mood,urgent},(result:any)=>{if(!result.ok){setSending(false);setToast(result.message)}})}const completed=history.filter(row=>["ended","matched"].includes(row.status)).length;return <section className="student-page">{toast&&<div className="student-toast"><CheckCircle/>{toast}</div>}<div className="student-welcome"><span>Welcome,</span><h1>{user?.displayName}</h1><p>{user?.rollNumber??"GBU Student"} · Gautam Buddha University</p><div><b>{completed} sessions completed</b></div></div>{waiting?<div className="waiting-card"><span className="student-spinner"/><div><h2>Waiting for a psychologist...</h2><p>Your request has been sent. Please stay on this page.</p></div></div>:<div className="request-card"><span><Heartbeat/></span><h2>Need to talk?</h2><p>Connect with a psychologist right now. You are not alone.</p><Button onClick={()=>setModal(true)}><ShieldCheck/> Request support</Button></div>}<div className="quick-access"><Link to="/student/resources"><Wind/><span><strong>Breathing exercise</strong>Calm your breathing</span></Link><Link to="/student/resources"><Lifebuoy/><span><strong>Helplines</strong>Call direct support</span></Link><Link to="/student/resources"><BookOpen/><span><strong>Grounding technique</strong>Return to the present</span></Link><Link to="/student/session"><Activity/><span><strong>Past sessions</strong>{completed} completed</span></Link></div>{modal&&<div className="student-modal-backdrop"><div className="student-modal"><button className="modal-close" onClick={()=>setModal(false)} aria-label="Close"><X/></button><h2>How are you feeling?</h2><p>This helps your psychologist support you better.</p><div className="mood-grid">{moods.map(item=><button key={item.label} className={mood===item.label?`selected ${item.tone}`:""} onClick={()=>setMood(item.label)}><span>{item.symbol}</span>{item.label}</button>)}</div><label className="urgency-row"><span><strong>Is this urgent?</strong><small>Priority matching</small></span><input type="checkbox" checked={urgent} onChange={e=>setUrgent(e.target.checked)}/><i/></label><Button disabled={!mood||sending} onClick={request}><ShieldCheck/>{sending?"Sending...":"Connect with psychologist"}</Button></div></div>}</section>}

export function StudentSession(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{api<any>("/student/history").then(r=>setRows(r.requests)).catch(()=>{})},[]);return <section className="student-page"><div className="simple-heading"><h1>My sessions</h1><p>Only session mode, status, and time are stored here. Conversation content is not retained.</p></div><div className="session-history">{rows.length?rows.map(row=><article key={row._id}><span className={`status status--${row.status}`}>{row.status}</span><div><strong>{row.mode} support session</strong><time>{new Date(row.createdAt).toLocaleString()}</time></div></article>):<div className="student-empty"><img src="/images/pschylogo.svg" alt=""/><h2>No sessions yet</h2><p>Your support history will appear here.</p><Link className="button button--primary" to="/student">Request support</Link></div>}</div></section>}

const phases=[{label:"Breathe in",seconds:4},{label:"Hold",seconds:4},{label:"Breathe out",seconds:6},{label:"Hold",seconds:2}];
export function StudentResources(){const[active,setActive]=useState(false);const[phase,setPhase]=useState(0);const[remaining,setRemaining]=useState(4);const[cycle,setCycle]=useState(0);useEffect(()=>{if(!active)return;setRemaining(phases[phase]!.seconds);const timer=setInterval(()=>setRemaining(value=>{if(value>1)return value-1;setPhase(current=>{const next=(current+1)%phases.length;if(next===0)setCycle(c=>c+1);return next});return phases[(phase+1)%phases.length]!.seconds}),1000);return()=>clearInterval(timer)},[active,phase]);const progress=active?1-remaining/phases[phase]!.seconds:0;const grounding=[[5,"See","Name five things you can see."],[4,"Touch","Notice four things you can feel."],[3,"Hear","Listen for three different sounds."],[2,"Smell","Find two things you can smell."],[1,"Taste","Notice one thing you can taste."]];const lines=[["Vandrevala Foundation","9999666555","General"],["iCall","9152987821","Students"],["Roshni Helpline","04066202000","Crisis"],["Sneha Foundation","04424640050","Crisis"],["Manas Foundation","01123389111","General"],["NIMHANS","08046804680","Govt"]];return <section className="student-page"><div className="simple-heading"><h1>Resources</h1><p>Small tools for grounding, breathing, and reaching direct help.</p></div><div className="resource-layout"><article className="breathing-card"><h2><Wind/> Box breathing exercise</h2>{active?<><div className="breathing-ring" style={{"--progress":`${progress*360}deg`} as any}><span><strong>{phases[phase]!.label}</strong>{remaining}s</span></div><p>Cycle {cycle+1}</p><Button variant="secondary" onClick={()=>setActive(false)}>Stop</Button></>:<><div className="breathing-idle"><Wind/></div><p>4-4-6-2 breathing pattern to reduce anxiety and stress.</p><Button onClick={()=>setActive(true)}>Start exercise</Button></>}</article><article className="grounding-card"><h2>5-4-3-2-1 grounding</h2>{grounding.map(([number,sense,text])=><div key={number}><b>{number}</b><span><strong>{sense}</strong>{text}</span></div>)}</article><article className="affirmation-card"><h2>Positive affirmations</h2>{["I am worthy of care and support.","This feeling is temporary. I will get through this.","I am stronger than I think.","It is okay to ask for help.","I deserve peace and happiness."].map(text=><p key={text}>{text}</p>)}</article><article className="helpline-card"><h2>Emergency helplines</h2>{lines.map(([name,number,tag])=><div key={name}><span><strong>{name}</strong><small className={`tag-${tag.toLowerCase()}`}>{tag}</small></span><a href={`tel:${number}`}><Phone/> Call</a></div>)}</article></div></section>}

export function StudentProfile(){const{user}=useAuth();const[rows,setRows]=useState<any[]>([]);useEffect(()=>{api<any>("/student/history").then(r=>setRows(r.requests)).catch(()=>{})},[]);const completed=rows.filter(row=>row.status==="ended").length;return <section className="student-page"><div className="profile-card"><div className="profile-cover"/><div className="profile-avatar"><GraduationCap/></div><h1>{user?.displayName}</h1><p>Student · Gautam Buddha University</p><div className="profile-info"><div><Mail/><span><small>Email</small><strong>{user?.email??"Not provided"}</strong></span></div>{user?.mobileNumber&&<div><Phone/><span><small>Phone</small><strong>{user.mobileNumber}</strong></span></div>}<div><GraduationCap/><span><small>Roll number</small><strong>{user?.rollNumber??"Not provided"}</strong></span></div><div><BookOpen/><span><small>Department</small><strong>{user?.department??"Not provided"}</strong></span></div></div><div className="profile-stats"><div><strong>{completed}</strong><span>Sessions completed</span></div><div><strong>Idle</strong><span>Current status</span></div></div></div></section>}
export const StudentHistory=StudentSession;
